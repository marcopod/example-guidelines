import LoadingSpinner from "@/public/svg/common/LoadingSpinner";

const Loading = () => {
  return (
    <div className="w-full h-full text-white bg-red-300">
      <LoadingSpinner />
      LOADING MODAFOKA
    </div>
  );
};

export default Loading;
